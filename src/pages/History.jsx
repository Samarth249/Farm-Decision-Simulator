import { Link } from 'react-router-dom'; import { useScenarios } from '../context/ScenarioContext';
export default function History() { const {scenarios,saveScenario,deleteScenario}=useScenarios(); return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-bold">Scenario history</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{scenarios.map(s=><article key={s.id} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between"><h2 className="font-semibold">{s.name}</h2><span className="rounded-full bg-amber-100 px-2 py-1 text-xs">{s.result.riskLevel}</span></div><p className="mt-2 text-sm text-slate-500">{s.inputs.location} • {s.inputs.crop} • {s.inputs.area} acres</p><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><span>Yield: <b>{s.result.yield} t/ha</b></span><span>Water: <b>{s.result.water} mm</b></span><span>Cost: <b>₹{s.result.cost.toLocaleString('en-IN')}</b></span><span>Profit: <b>₹{s.result.profit.toLocaleString('en-IN')}</b></span></div><div className="mt-5 flex gap-3 text-sm"><Link to="/compare" className="text-emerald-700">View / Compare</Link><button onClick={()=>saveScenario({...s,name:`${s.name} copy`})} className="text-blue-700">Duplicate</button><button onClick={()=>deleteScenario(s.id)} className="text-red-600">Delete</button></div></article>)}</div></div>; }
